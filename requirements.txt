# F1 Aerodynamic Design Pipeline - Dependencies
# Install: pip install -r requirements.txt
# For GPU support: pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu121

# === Core (existing pipeline) ===
numpy>=1.24
scipy>=1.11
matplotlib>=3.7
meshio>=5.3
pyvista>=0.42
numpy-stl>=3.0

# === NVIDIA Omniverse / USD ===
# OpenUSD Python bindings for STL -> USD geometry conversion
# Note: Omniverse Kit SDK is installed separately via NVIDIA launcher
usd-core>=25.08

# === NVIDIA Modulus (Physics-ML) ===
# Physics-informed neural networks, Fourier Neural Operators, DeepONet
# Requires CUDA-capable GPU for training; CPU inference supported
nvidia-modulus>=0.6.0

# === PyTorch (backend for Modulus + ML surrogates) ===
torch>=2.6.0

# === ML Surrogate Models ===
scikit-learn>=1.3        # Baseline models, polynomial features, metrics
gpytorch>=1.11           # Gaussian Process regression with CUDA support
botorch>=0.9             # Bayesian optimization on top of GPyTorch

# === Karpathy's Autoresearch ===
# Automated ML research: iterative experiment design, execution, improvement
aresearch>=0.1.0

# === Utilities ===
pyyaml>=6.0              # Configuration file loading
pandas>=2.0              # Data analysis and experiment tracking
tqdm>=4.65               # Progress bars for training loops
